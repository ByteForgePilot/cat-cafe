# 猫咖点单系统 — 开发日志

## 环境信息

| 项目 | 版本/说明 |
|------|-----------|
| 数据库 | MySQL 8.0.45 |
| 安装参考 | [MySQL Installer 下载](https://downloads.mysql.com/archives/installer/) |
| 安装教程 | [知乎专栏](https://zhuanlan.zhihu.com/p/654087404) |

## 数据库设计

### 表结构（6 张表）

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| user | 用户 | userType: 0=顾客, 1=管理员 |
| catinformation | 猫咪信息 | status: 0=休息中, 1=在岗中 |
| product | 商品 | category: 0=服务 1=餐饮 2=猫咪用品, status: 0=下架 1=在售 |
| orders | 订单 | batchNo 批次号, orderStatus 五态, 用户名手机号快照 |
| comment | 评论 | targetType: 0=商品 1=猫咪, auditStatus: 0=待审核 1=已通过 2=已拒绝 |
| likes | 点赞 | likeType: 0=商品 1=评论 2=猫咪, 联合唯一索引防重复点赞 |

### 相对原始设计的结构变更

- **product 表 — 新增字段**
  - 新增 `status TINYINT`
  - 原因：支持上架(1) / 下架(0)。有历史订单时不能直接删商品，先下架隐藏

- **orders 表 — 新增字段**
  - 新增 `batchNo VARCHAR(32)`
  - 原因：多商品下单时同一批次共用，支持聚合展示、批量取消/支付

- **comment 表 — 字段替换**
  - 删除 `catid`，替换为 `targetType` + `targetId`
  - 原因：一张表同时评猫咪(targetType=1)和商品(targetType=0)，和 likes 设计统一

- **comment 表 — 移除约束**
  - 移除外键 `FK → catinformation`，改为软引用
  - 原因：targetId 可能指向 product 或 catinformation，无法建单一外键

---

---

## 后端

### 技术栈

| 项目 | 版本/说明 |
|------|-----------|
| Python | 3.13.0（官方，非 Anaconda） |
| 虚拟环境 | venv（位于 `backend/.venv/`） |
| Web 框架 | FastAPI 0.115.6 |
| 服务器 | Uvicorn 0.34.0 |
| ORM | SQLAlchemy 2.0.36 |
| MySQL 驱动 | PyMySQL 1.1.1 |
| 数据校验 | Pydantic 2.10.5（FastAPI 自带） |
| 环境变量 | python-dotenv 1.0.1 |

### 调用链

```
客户端请求 → Uvicorn → FastAPI → SQLAlchemy → PyMySQL → MySQL
```

### 项目结构

```
backend/
├── .venv/              # 虚拟环境（不上传 Git）
├── .env                # 数据库密码等敏感信息（不上传 Git）
├── requirements.txt    # 依赖清单
├── main.py             # FastAPI 应用入口
├── config.py           # 读取 .env，拼接数据库连接字符串
├── database.py         # 数据库引擎 + 会话工厂 + Base 基类
└── models.py           # 6 张表的 ORM 模型（Python 类 → 数据库表的映射）
```

### 各文件作用

- **`.env`** — 存五条配置（数据库地址、端口、用户名、密码、库名）。写在这里而不是代码里，被 `.gitignore` 排除，不会泄露到 GitHub。

- **`config.py`** — 用 `python-dotenv` 读取 `.env` 文件内容，拼接出标准格式的数据库连接字符串 `mysql+pymysql://root:yang@localhost:3306/cat_cafe`，供 `database.py` 使用。

- **`database.py`** — 核心连接模块：
  - `engine`：全局唯一的数据库引擎（连接池），程序启动时创建一次，所有请求共用
  - `SessionLocal`：会话工厂，每次请求"取号"拿一个新会话，用完归还
  - `Base`：ORM 基类，`models.py` 中所有表模型都继承它
  - `get_db()`：FastAPI 依赖注入函数，请求进来创建会话，结束自动关闭

- **`main.py`** — FastAPI 应用入口：
  - 创建 `FastAPI` 实例（`title="猫咖点单系统"`）
  - 当前仅包含 `/health` 健康检查接口，验证服务是否跑起来
  - 启动命令：`uvicorn main:app --reload`
  - 建表方式：采用 `init.sql` 手动建表，不调用 `create_all`。原因 —— `@app.on_event("startup")` 已弃用；`init.sql` 作为权威建表文档，组员无需安装 Python 也能查看；保证数据库结构完全可控

- **`models.py`** — ORM 模型层，6 个类对应 6 张表（`User` / `Catinformation` / `Product` / `Order` / `Comment` / `Likes`）。每个类属性一对一映射数据库字段（camelCase）：

  | SQL 类型 | Python Column |
  |----------|---------------|
  | `BIGINT UNSIGNED` | `BigInteger` |
  | `VARCHAR(n)` | `String(n)` |
  | `TEXT` | `Text` |
  | `DECIMAL(10,2)` | `Numeric(10, 2)` |
  | `DATE` | `Date` |
  | `DATETIME` | `DateTime` |
  | `NOT NULL` | `nullable=False` |
  | `DEFAULT 0` | `default=0` |
  | `DEFAULT CURRENT_TIMESTAMP` | `server_default=func.now()` |
  | `PRIMARY KEY` | `primary_key=True` |
  | `UNIQUE` | `unique=True` |
  | 外键约束 | `ForeignKey("表.字段")` |

  两个关键概念：
  - **`Foreign Field`**：数据库层面的硬约束，对等 init.sql 的 `CONSTRAINT ... FOREIGN KEY ... REFERENCES`。阻止插入不存在的关联 ID，`ondelete="RESTRICT"` 阻止删有关联数据的记录
  - **`relationship`**：Python 层面的快捷方式，不产生数据库字段。用来在代码中链式访问关联对象 —— `order.user.userName`（订单 → 用户 → 用户名），`user.orders`（用户 → 他的所有订单），避免每次手写 JOIN

  注意：`models.py` 不负责建表。建表统一用 `database/init.sql`，`models.py` 仅做运行时映射。

- **`schemas.py`** — Pydantic 数据校验模型，定义每个接口的请求和响应格式。`ConfigDict(from_attributes=True)` 使其可直接从 ORM 对象读取属性。

---
## 后端测试
管理员cmd运行  net start MySQL80 打开数据库服务


启动后端（包括激活环境）


cd G:\cat-cafe\backend


.venv\Scripts\activate


uvicorn main:app --reload

两个终端都不关，打开  http://127.0.0.1:8000/docs 测试
## API 接口清单

### 用户模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/api/register` | 用户注册 | 已完成：正常注册、密码过短、用户已存在、用户名为空 待完成：手机验证、跳过个人信息后补填 |
| POST | `/api/login` | 用户登录 | 已完成：正常登录、用户名或密码错误 待完成：登录态维持 |
| GET | `/api/user/me` | 查看自己信息 | 待完成 |
| PUT | `/api/user/me` | 修改自己信息 | 待完成：密码、性别、生日等 |
| POST | `/api/reset-password` | 找回密码 | 待完成 |
| DELETE | `/api/user/me` | 注销账户 | 待完成 |
| GET | `/api/admin/users` | 管理员查所有用户 | 待完成 |
| DELETE | `/api/admin/users/{userId}` | 管理员删除用户 | 待完成 |

### 商品模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/api/products` | 浏览商品 | 待完成：按 category 筛选、只看在售 status=1 |
| GET | `/api/products/{productId}` | 商品详情 | 待完成 |
| POST | `/api/admin/products` | 上架商品 | 待完成 |
| PUT | `/api/admin/products/{productId}` | 修改商品信息 | 待完成 |
| PUT | `/api/admin/products/{productId}/off` | 下架商品 | 待完成：status 改为 0 |

### 订单模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/api/orders` | 下单 | 待完成：支持 batchNo 多商品、扣库存 |
| GET | `/api/orders` | 查看自己订单 | 待完成：按状态筛选、按商品名查 |
| GET | `/api/orders/{orderId}` | 订单详情 | 待完成 |
| PUT | `/api/orders/{orderId}` | 修改订单 | 待完成：用户改自己（限状态）、管理员改所有 |
| DELETE | `/api/orders/{orderId}` | 删除订单 | 待完成：用户删自己、管理员删所有 |

### 猫咪模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| GET | `/api/cats` | 查看猫咪列表 | 待完成：只看在岗 status=1 |
| GET | `/api/cats/{catId}` | 猫咪详情 | 待完成 |
| POST | `/api/admin/cats` | 新增猫咪 | 待完成 |
| PUT | `/api/admin/cats/{catId}` | 修改猫咪信息 | 待完成 |
| DELETE | `/api/admin/cats/{catId}` | 删除猫咪 | 待完成 |

### 评论模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/api/comments` | 发表评论 | 待完成：targetType=0商品/1猫咪 |
| GET | `/api/comments` | 查看评论 | 待完成：按对象筛选、仅看已通过审核 |
| PUT | `/api/admin/comments/{commentId}/audit` | 审核评论 | 待完成：通过/退回 |
| DELETE | `/api/comments/{commentId}` | 删除评论 | 待完成：发评人或管理员可删 |

### 点赞模块

| 方法 | 路径 | 功能 | 说明 |
|------|------|------|------|
| POST | `/api/likes` | 点赞 | 待完成：likeType=0商品/1评论/2猫咪，重复点赞拒绝 |
| DELETE | `/api/likes/{likeId}` | 取消点赞 | 待完成 |
| GET | `/api/likes` | 查看我的点赞 | 待完成：按 likeType 筛选 |

## 错误码一览

| 状态码 | 含义 | 触发场景 |
|--------|------|----------|
| 200 | 成功 | 注册/登录正常 |
| 401 | 认证失败 | 用户名不存在或密码错误 |
| 422 | 请求数据不合规 | 用户名已存在、字段为空、长度超限 |
| 500 | 服务器内部错误 | 数据库未启动、服务异常 |


### POST /api/register — 用户注册

- **请求**：`{ "userName": "str", "userPassword": "str" }`
- **响应**：`{ "userId": int, "userName": "str", "userType": 0, "registerTime": datetime }`
- **错误**：422 `"用户名已存在"`
- **说明**：密码 bcrypt 哈希存储，所有用户默认注册为顾客(userType=0)


### POST /api/login — 用户登录

- **请求**：`{ "userName": "str", "userPassword": "str" }`
- **响应**：`{ "userId": int, "userName": "str", "userType": int, "registerTime": datetime }`
- **错误**：401 `"用户名或密码错误"`
- **说明**：用 `pwd_context.verify()` 比对密码哈希，不存在和密码错统一提示

